# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|

## User Preferences
- User prefers concise, step-by-step execution updates during implementation.

## Patterns That Work
- In this repo, theme adjustments are made directly in JSON theme files at the repo root.
- When syncing this repo to `~/.config/openchamber/themes`, safely replace the existing directory by moving it to a timestamped backup first, then symlink the repo root.

## Patterns That Don't Work
- Searching for app source locally does not help; this repo only contains theme JSON and docs.

## Domain Notes
- This repository stores custom OpenChamber theme files and theme docs, not the full OpenChamber application source.
